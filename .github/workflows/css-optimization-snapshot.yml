name: CSS optimization snapshot

on:
  push:
    branches:
      - chore/css-ownership-optimization

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: css-optimization-snapshot
          path: .
          include-hidden-files: true
          retention-days: 1
