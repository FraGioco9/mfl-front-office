name: Route dependency validator ownership migration

on:
  push:
    branches:
      - refactor/canonical-route-dependency-plan
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/canonical-route-dependency-plan
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Update route dependency ownership validators
        run: node site/migrate-route-dependency-validator-ownership.mjs
      - name: Remove temporary validator migration scaffolding
        shell: bash
        run: |
          rm -f site/migrate-route-dependency-validator-ownership.mjs
          rm -f .github/workflows/route-dependency-validator-ownership-migration.yml
      - name: Persist validator ownership migration
        shell: bash
        run: |
          set -euo pipefail
          git add -A
          if git diff --cached --quiet; then
            echo "Route dependency validators are already canonical."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Update route dependency ownership validators"
          git push origin HEAD:refactor/canonical-route-dependency-plan
