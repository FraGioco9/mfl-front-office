name: Temporary issue 376 Discount Rate render signature patch

on:
  push:
    branches:
      - fix/issue-376-discount-rate-render-signature
    paths:
      - .github/workflows/tmp-issue-376-discount-rate-render-signature.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/issue-376-discount-rate-render-signature
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply source fix
        working-directory: site
        run: node patch-issue-376-discount-rate-render-signature.mjs
      - name: Install dependencies
        working-directory: site
        run: npm install --no-audit --no-fund
      - name: Build canonical application core
        working-directory: site
        run: npm run build:core
      - name: Lint
        working-directory: site
        run: npm run lint
      - name: Typecheck
        working-directory: site
        run: npm run typecheck
      - name: Validate repository
        working-directory: site
        run: npm run validate
      - name: Commit fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add site/modules/app-core.js site/modules/app-core-runtime.js site/modules/app-core-evaluation-runtime.js site/validate-evaluation-discount-derived-loading.mjs
          git commit -m "fix: rerender Evaluation across Discount Rate pending state"
          git push origin HEAD:fix/issue-376-discount-rate-render-signature
