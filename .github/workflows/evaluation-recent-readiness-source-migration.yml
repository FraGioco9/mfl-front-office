name: Evaluation recent readiness source migration

on:
  push:
    branches:
      - refactor/source-owned-evaluation-recent-readiness
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/source-owned-evaluation-recent-readiness
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply source-owned Evaluation recent readiness migration
        run: node site/migrate-source-owned-evaluation-recent-readiness.mjs
      - name: Persist migrated source
        shell: bash
        run: |
          set -euo pipefail
          git add -A
          if git diff --cached --quiet; then
            echo "Evaluation recent readiness is already source-owned."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Move Evaluation recent readiness into source"
          git push origin HEAD:refactor/source-owned-evaluation-recent-readiness
