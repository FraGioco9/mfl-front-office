name: Issue 594 core source migration

on:
  push:
    branches:
      - refactor/594-global-runtime-ownership
    paths:
      - site/migrate-594-core-sources.mjs
      - site/migrate-594-source-validators.mjs
      - site/migrate-594-validator-fixes.mjs
      - .github/workflows/issue594-core-source-migration.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: refactor/594-global-runtime-ownership
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: site/package-lock.json
      - name: Promote shipped core artifacts to canonical sources
        run: node site/migrate-594-core-sources.mjs
      - name: Migrate source ownership validators
        run: node site/migrate-594-source-validators.mjs
      - name: Apply source validator fixes
        run: node site/migrate-594-validator-fixes.mjs
      - name: Generate source-owned core artifacts
        working-directory: site
        run: node build-app-core.mjs
      - name: Commit source-owned core migration locally
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A site
          git commit -m "Make split application cores source-owned"
      - name: Install dependencies
        working-directory: site
        run: npm ci
      - name: Verify source-owned core build
        working-directory: site
        run: npm run check
      - name: Remove migration helpers and push
        run: |
          git rm site/migrate-594-core-sources.mjs site/migrate-594-source-validators.mjs site/migrate-594-validator-fixes.mjs .github/workflows/issue594-core-source-migration.yml
          git commit -m "Remove issue 594 core migration helpers"
          git push origin HEAD:refactor/594-global-runtime-ownership
