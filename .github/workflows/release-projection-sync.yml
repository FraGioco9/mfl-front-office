name: Release projection sync

on:
  pull_request:
    paths:
      - site/release.json
      - site/sync-release-projections.mjs
      - site/build-app-core.mjs
      # Canonical UI metadata changes can alter generated first-paint HTML.
      - site/modules/app-config.js
      - site/patch-retired-busy-validators.mjs
      - site/bootstrap.js
      - site/bootstrap-core.js
      - site/index.html
      - .github/workflows/release-projection-sync.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release-projection-sync-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  sync:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Update retired busy validators
        run: node site/patch-retired-busy-validators.mjs

      - name: Normalize release projections
        run: node site/sync-release-projections.mjs

      - name: Commit validator cleanup
        shell: bash
        env:
          HEAD_BRANCH: ${{ github.head_ref || github.ref_name }}
        run: |
          set -euo pipefail
          git add -- site/validate.mjs site/validate-z-index-ownership.mjs site/validate-stats-animation-owner.mjs site/validate-modal-entrance-lifecycle.mjs site/bootstrap.js site/bootstrap-core.js site/index.html
          if git diff --cached --quiet; then
            echo "Retired busy validators and release projections are current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Update retired busy validators"
          git push origin "HEAD:$HEAD_BRANCH"
