name: Issue 866 Development Center safety hardening

on:
  push:
    branches:
      - fix/866-development-center-club-reuse

permissions:
  contents: write

jobs:
  harden:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/866-development-center-club-reuse
          fetch-depth: 1
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Apply permanent hardening
        run: python .github/issue-866-development-center-hardening.py
      - name: Check syntax
        run: python -m py_compile scripts/database/clubs.py tests/test_database_refresh_controls.py
      - name: Run refresh-control regressions
        run: python -m unittest tests.test_database_refresh_controls
      - name: Commit permanent changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add scripts/database/clubs.py tests/test_database_refresh_controls.py
          git rm .github/issue-866-development-center-hardening.py .github/workflows/issue-866-development-center-hardening.yml
          git commit -m 'Keep Development Center club reuse validation fail-closed'
          git push
