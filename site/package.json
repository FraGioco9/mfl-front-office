{
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build:core": "node build-app-core.mjs",
    "build": "npm run build:core",
    "lint": "eslint bootstrap.js build-app-core.mjs modules api/releases.js api/mfl-season-ratios-v2.js *-runtime.js validate.mjs validate-route-runtime.mjs",
    "typecheck": "tsc -p jsconfig.json --noEmit",
    "validate": "node validate.mjs && node validate-route-runtime.mjs",
    "check": "npm run lint && npm run typecheck && npm run build:core && npm run validate"
  },
  "dependencies": {
    "@onflow/fcl": "1.21.11"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^16.0.0",
    "typescript": "^5.0.0"
  }
}
